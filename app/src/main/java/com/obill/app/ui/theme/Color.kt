package com.obill.app.ui.theme

import androidx.compose.ui.graphics.Color

// Palet biru muda untuk kesan modern/profesional
val OrangeBrand = Color(0xFF6EC6FF) // dipakai sebagai warna gradient start
val BlueBrand = Color(0xFF2F6BFF)   // dipakai sebagai warna gradient end
val PageBackground = Color(0xFFEAF6FF)
/** Sama dengan `colors.xml` `splash_background` — agar transisi dari splash sistem ke Compose mulus */
val SplashBackground = Color(0xFFEFEEF3)
val CardSurface = Color(0xFFFFFFFF)
